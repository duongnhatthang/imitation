---
phase: 04-full-run-and-analysis
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - experiments/plot_config.py
  - experiments/analyze_results.py
  - tests/test_analysis.py
autonomous: true
requirements: [EVAL-01, EVAL-04, EVAL-05, EVAL-07]

must_haves:
  truths:
    - "Normalized scores computed as (agent - random) / (expert - random) for each run"
    - "Script handles missing/partial/empty Sacred output without crashing"
    - "Dashboard shows correct counts of done/running/pending/failed runs"
    - "All algorithms use consistent colors and line styles defined in one place"
  artifacts:
    - path: "experiments/plot_config.py"
      provides: "Shared COLOR_MAP, LINESTYLE_MAP, RCPARAMS for consistent figure styling"
      exports: ["COLOR_MAP", "LINESTYLE_MAP", "LINEWIDTH_MAP", "RCPARAMS", "ALGORITHMS", "ALGO_DISPLAY_NAMES"]
    - path: "experiments/analyze_results.py"
      provides: "CLI with dashboard subcommand, Sacred output reader, score normalization"
      min_lines: 150
    - path: "tests/test_analysis.py"
      provides: "Tests for Sacred parsing, normalization, dashboard, incremental handling"
      min_lines: 80
  key_links:
    - from: "experiments/analyze_results.py"
      to: "experiments/plot_config.py"
      via: "import COLOR_MAP, LINESTYLE_MAP, RCPARAMS"
      pattern: "from experiments.plot_config import"
    - from: "experiments/analyze_results.py"
      to: "output/sacred/{TIMESTAMP}/{algo}/{game}/{seed}/*/metrics.json"
      via: "pathlib.glob for Sacred output discovery"
      pattern: "glob.*run.json"
    - from: "experiments/analyze_results.py"
      to: "experiments/atari_helpers.py"
      via: "import ATARI_GAMES"
      pattern: "from experiments.atari_helpers import"
---

<objective>
Create the analysis foundation: plot configuration, Sacred output reader, normalized score computation, completion dashboard, and test scaffolding with mock Sacred output fixtures.

Purpose: Establish the data loading layer and consistent styling that all figure generation (Plan 02) depends on. The dashboard (EVAL-05) gives immediate visibility into benchmark progress.

Output: `experiments/plot_config.py`, `experiments/analyze_results.py` (with `dashboard` subcommand and data loading functions), `tests/test_analysis.py` with mock Sacred fixtures.
</objective>

<execution_context>
@/Users/thangduong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thangduong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-full-run-and-analysis/04-RESEARCH.md

@experiments/atari_helpers.py
@experiments/run_atari_experiment.py
@experiments/run_atari_benchmark.sh

<interfaces>
<!-- Key types and contracts the executor needs from existing code -->

From experiments/atari_helpers.py:
```python
ATARI_GAMES = {
    "Pong": "PongNoFrameskip-v4",
    "Breakout": "BreakoutNoFrameskip-v4",
    "BeamRider": "BeamRiderNoFrameskip-v4",
    "Enduro": "EnduroNoFrameskip-v4",
    "Qbert": "QbertNoFrameskip-v4",
    "Seaquest": "SeaquestNoFrameskip-v4",
    "SpaceInvaders": "SpaceInvadersNoFrameskip-v4",
}

def compute_normalized_score(agent_score: float, random_score: float, expert_score: float) -> float:
```

From experiments/run_atari_experiment.py (Sacred config defaults):
```python
algo = "dagger"          # One of: bc, dagger, ftrl
game = "Pong"            # One of the 7 ATARI_GAMES keys
seed = 0
n_rounds = 20
total_timesteps = 500000
output_dir = "output/sacred"
```

Sacred output directory structure (from run_atari_benchmark.sh):
- Observer path: `output/sacred/{TIMESTAMP}/{algo}/{game}/{seed}/`
- Each observer dir contains run subdirectories: `1/run.json`, `1/config.json`, `1/metrics.json`
- TIMESTAMP is ISO-8601 from `common.sh`

Sacred metrics.json format (from RESEARCH.md):
```json
{
  "normalized_score": {
    "values": [0.1, 0.2, ...],
    "steps": [0, 1, ...],
    "timestamps": ["2026-...", ...]
  }
}
```

Sacred run.json status values: "COMPLETED", "RUNNING", "FAILED", "INTERRUPTED", "QUEUED"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plot_config.py and analyze_results.py with Sacred reader, dashboard, and CLI</name>
  <files>experiments/plot_config.py, experiments/analyze_results.py</files>
  <read_first>
    experiments/atari_helpers.py
    experiments/run_atari_experiment.py
    experiments/run_atari_benchmark.sh
    experiments/common.sh
    .planning/phases/04-full-run-and-analysis/04-RESEARCH.md
  </read_first>
  <action>
Create `experiments/plot_config.py` with:
- `ALGORITHMS = ["BC", "DAgger", "FTRL", "Expert"]` (display names)
- `ALGO_KEYS = ["bc", "dagger", "ftrl"]` (Sacred config keys, no "Expert" since it is a reference line)
- `SEEDS = [0, 1, 2]`
- `COLOR_MAP` using seaborn colorblind palette: `dict(zip(ALGORITHMS, sns.color_palette("colorblind", n_colors=4)))`
- `LINESTYLE_MAP = {"BC": "-", "DAgger": "--", "FTRL": "-.", "Expert": ":"}`
- `LINEWIDTH_MAP = {"BC": 1.5, "DAgger": 2.0, "FTRL": 2.0, "Expert": 1.0}`
- `ALGO_DISPLAY_NAMES = {"bc": "BC", "dagger": "DAgger", "ftrl": "FTRL"}` (mapping Sacred keys to display names)
- `RCPARAMS` dict with: font.size=11, axes.labelsize=12, axes.titlesize=12, legend.fontsize=10, xtick.labelsize=10, ytick.labelsize=10, figure.dpi=150, savefig.dpi=300, savefig.format="pdf", pdf.fonttype=42, ps.fonttype=42, figure.figsize=(10,6), axes.grid=True, grid.alpha=0.3
- `apply_rcparams()` function that calls `plt.rcParams.update(RCPARAMS)`

Create `experiments/analyze_results.py` with:

1. **Sacred output loading** (`load_sacred_run(obs_path: Path) -> dict | None`):
   - Find latest run subdirectory: `sorted(obs_path.glob("*/run.json"), key=lambda p: int(p.parent.name))`
   - If no run.json found, return None
   - Read run.json for status; include runs with status COMPLETED or RUNNING (EVAL-04 incremental)
   - Read config.json for n_rounds, total_timesteps
   - Read metrics.json for normalized_score values and steps
   - Return dict: `{"status": str, "config": dict, "rounds": list[int], "normalized_scores": list[float]}`

2. **Collect all results** (`collect_results(output_dir: Path) -> dict`):
   - Walk `output_dir/{algo}/{game}/{seed}/` for all algo in `ALGO_KEYS`, game in `ATARI_GAMES.keys()`, seed in `SEEDS`
   - Call `load_sacred_run` for each
   - Return dict keyed by `(algo_key, game, seed)` -> run data
   - Handle missing directories gracefully (skip, do not crash)

3. **Dashboard** (`print_dashboard(results: dict, output_dir: Path)`):
   - Cross-reference all 63 expected combinations (3 algos x 7 games x 3 seeds)
   - Count COMPLETED, RUNNING, FAILED/INTERRUPTED, PENDING (not found)
   - Print summary line: `"Done: N  Running: N  Failed: N  Pending: N / 63"`
   - Print per-game table with algo columns and seed rows showing status emoji or abbreviation
   - Save to `figures/dashboard.txt`

4. **CLI** using argparse with subcommands:
   - `dashboard` subcommand: `--output-dir` (default: auto-detect latest `output/sacred/*/` by mtime)
   - `curves` subcommand: placeholder that prints "Not yet implemented" (Plan 02)
   - `aggregate` subcommand: placeholder that prints "Not yet implemented" (Plan 02)
   - Auto-detect output dir: `sorted(Path("output/sacred").glob("*"), key=lambda p: p.stat().st_mtime)[-1]` if --output-dir not given

5. **sys.path setup** at top: same pattern as atari_smoke.py (`_PROJECT_ROOT = Path(__file__).resolve().parents[1]`)

IMPORTANT: The observer path in run_atari_benchmark.sh is `output/sacred/${TIMESTAMP}/{algo}/{game}/{seed}` so the `--output-dir` should point to `output/sacred/{TIMESTAMP}/` (the timestamped parent). The `collect_results` function walks `{output_dir}/{algo}/{game}/{seed}/`.

BC has only one round entry (step=0). DAgger/FTRL have entries for rounds 0..N-1. Handle both cases in loading.
  </action>
  <verify>
    <automated>cd /Users/thangduong/Desktop/imitation && python -c "from experiments.plot_config import COLOR_MAP, LINESTYLE_MAP, RCPARAMS, ALGORITHMS, ALGO_DISPLAY_NAMES; assert len(COLOR_MAP) == 4; assert len(RCPARAMS) > 10; print('plot_config OK')" && python -c "from experiments.analyze_results import load_sacred_run, collect_results, print_dashboard; print('analyze_results imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `experiments/plot_config.py` exists and exports COLOR_MAP with 4 entries (BC, DAgger, FTRL, Expert)
    - `experiments/plot_config.py` exports RCPARAMS with pdf.fonttype=42
    - `experiments/analyze_results.py` exists with functions: load_sacred_run, collect_results, print_dashboard
    - `python experiments/analyze_results.py dashboard --output-dir /nonexistent` does not crash (prints "No runs found" or similar)
    - `from experiments.plot_config import COLOR_MAP, LINESTYLE_MAP, ALGORITHMS` succeeds
  </acceptance_criteria>
  <done>plot_config.py provides consistent styling constants; analyze_results.py loads Sacred output, computes dashboard, handles missing/partial data without crashing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test_analysis.py with mock Sacred fixtures and tests for EVAL-01, EVAL-04, EVAL-05, EVAL-07</name>
  <files>tests/test_analysis.py</files>
  <read_first>
    experiments/plot_config.py
    experiments/analyze_results.py
    experiments/atari_helpers.py
    tests/conftest.py
    .planning/phases/04-full-run-and-analysis/04-RESEARCH.md
  </read_first>
  <behavior>
    - test_normalized_score: compute_normalized_score(150, 50, 250) == 0.5; compute_normalized_score(50, 50, 250) == 0.0 (random-level); compute_normalized_score(250, 50, 250) == 1.0 (expert-level); compute_normalized_score(100, 100, 100) == 0.0 (division-by-zero guard)
    - test_load_sacred_run_completed: Mock Sacred dir with COMPLETED run.json and metrics.json containing normalized_score values [0.1, 0.3, 0.5] at steps [0, 1, 2] -- load_sacred_run returns correct dict with status="COMPLETED" and all three scores
    - test_load_sacred_run_running: Mock Sacred dir with RUNNING status -- load_sacred_run returns partial data (EVAL-04 incremental)
    - test_load_sacred_run_failed: Mock Sacred dir with FAILED status -- load_sacred_run returns None (skip failed runs)
    - test_load_sacred_run_missing: Non-existent path -- load_sacred_run returns None without crashing
    - test_collect_results_partial: Create mock output with only 2 of 63 runs present -- collect_results returns dict with 2 entries, no crash
    - test_collect_results_empty: Empty output dir -- collect_results returns empty dict
    - test_dashboard_counts: Given results dict with 2 COMPLETED, 1 RUNNING, 0 FAILED -- dashboard correctly counts 2 done, 1 running, 0 failed, 60 pending
    - test_consistent_colors: All algorithms in ALGORITHMS have entries in COLOR_MAP, LINESTYLE_MAP, LINEWIDTH_MAP (EVAL-07)
    - test_plot_config_rcparams: RCPARAMS contains pdf.fonttype=42 and savefig.dpi=300
  </behavior>
  <action>
Create `tests/test_analysis.py`:

1. **Fixture `mock_sacred_output` (tmp_path-based):**
   - Create directory structure: `{tmp_path}/{algo}/{game}/{seed}/1/`
   - Write `run.json` with `{"status": "COMPLETED", "start_time": "...", "stop_time": "..."}`
   - Write `config.json` with `{"algo": "dagger", "game": "Pong", "seed": 0, "n_rounds": 20, "total_timesteps": 500000}`
   - Write `metrics.json` with `{"normalized_score": {"values": [0.1, 0.3, 0.5], "steps": [0, 1, 2], "timestamps": ["t1", "t2", "t3"]}}`
   - Helper function `_create_mock_run(base_dir, algo, game, seed, status, scores, steps)` to create runs with different statuses

2. **Tests** (all prefixed with `test_`):
   - Use `from experiments.atari_helpers import compute_normalized_score` for EVAL-01 tests
   - Use `from experiments.analyze_results import load_sacred_run, collect_results, print_dashboard` for EVAL-04/05 tests
   - Use `from experiments.plot_config import COLOR_MAP, LINESTYLE_MAP, LINEWIDTH_MAP, RCPARAMS, ALGORITHMS` for EVAL-07 tests
   - Each test function is independent and uses tmp_path fixture for isolation

3. **sys.path setup** at top: same pattern as other test files (`_PROJECT_ROOT = Path(__file__).resolve().parents[1]`)

Run tests with: `pytest tests/test_analysis.py -x -q`
  </action>
  <verify>
    <automated>cd /Users/thangduong/Desktop/imitation && python -m pytest tests/test_analysis.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_analysis.py` exists with at least 10 test functions
    - `pytest tests/test_analysis.py -x -q` passes all tests (0 failures)
    - Tests cover: normalized score computation, Sacred run loading (completed/running/failed/missing), collect_results with partial data, dashboard counts, consistent colors/styles
    - Each test uses tmp_path for isolation (no shared state)
  </acceptance_criteria>
  <done>All tests pass; Sacred reader correctly handles COMPLETED, RUNNING, FAILED, and missing runs; dashboard counts are accurate; plot_config consistency verified</done>
</task>

</tasks>

<verification>
- `python -c "from experiments.plot_config import COLOR_MAP; assert len(COLOR_MAP) == 4"` -- consistent styling defined
- `python -c "from experiments.analyze_results import collect_results"` -- Sacred reader importable
- `pytest tests/test_analysis.py -x -q` -- all tests pass
- `python experiments/analyze_results.py dashboard --output-dir /tmp/empty` -- does not crash on empty directory
</verification>

<success_criteria>
- plot_config.py exports consistent COLOR_MAP, LINESTYLE_MAP, RCPARAMS used across all figures (EVAL-07)
- analyze_results.py reads Sacred output directories and computes normalized scores (EVAL-01)
- Dashboard shows completion status for all 63 expected (algo, game, seed) combinations (EVAL-05)
- All functions handle missing/partial/empty Sacred output without crashing (EVAL-04)
- Tests verify all of the above with mock Sacred fixtures
</success_criteria>

<output>
After completion, create `.planning/phases/04-full-run-and-analysis/04-01-SUMMARY.md`
</output>
